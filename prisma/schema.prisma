datasource db {
  url      = env("DATABASE_URL")
  provider = "mongodb"
}

generator client {
  provider = "prisma-client-js"
  previewFeatures = ["mongodb"]
}

model UserWithInfractions {
  id          String @id @map("_id")
  infractions String[]
}